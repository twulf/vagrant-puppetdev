---
:backends:
  - yaml
:hierarchy:
  - global
  - "nodes/%{::clientcert}"
  - "types/%{::server_type}/%{::environment}"
  - "types/%{::server_type}"
  - "locations/%{::location}"
  - "environments/%{::environment}"
  - defaults
:yaml:
  :datadir: /tmp/hieradata
  :merge_behavior: deeper
